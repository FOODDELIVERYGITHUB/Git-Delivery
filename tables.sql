create table Billadd(
    Full_name varchar(255),
    Email varchar(255),
    Address varchar(255),
    City varchar(255)
);

create table Card_det(
    Name varchar(255),
    Card_num varchar(255),
    Exp_month varchar(255),
    CVV varchar(255),
    Exp_year year
);
